create an environment with python3 -m venv wormhole-env
activate the environment with source wormhole-env/bin/activate
create env files
build with docker container
run with docker container or run the app.py
